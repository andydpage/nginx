apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "andy.fullname" . }}-test-connection"
  labels:
{{ include "andy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "andy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
